<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aasshh.user">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.SingleCategoryProduct"></activity>
        <activity android:name=".ui.NewPasswordSet" />
        <activity android:name=".ui.OtpVerification" />
        <activity android:name=".ui.ForgetPassword" />
        <activity android:name=".ui.signup.CreateProfile" />
        <activity android:name=".ui.signup.VerifyOtp" />
        <activity android:name=".ui.signup.SignupGetStarted" />
        <activity android:name=".ui.Login" />
        <activity android:name=".ui.Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>